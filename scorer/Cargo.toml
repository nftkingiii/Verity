[package]
name = "verity-scorer"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Deterministic scorer for Verity's ONCHAIN_TX_LOOKUP canonical payload."

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = true
